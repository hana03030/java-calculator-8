package calculator;

public class StringAddCalculator {

    public static int splitAndSum(String input) {
        // 입력이 비어 있으면 0 변환
        if (input == null || input.isEmpty()) {
            return 0;
        }

        // 다음 단계에서 구분자 처리 로직을 추가할 예정
        return 0;
    }
}
